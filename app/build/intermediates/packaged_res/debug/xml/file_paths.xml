<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Access to the downloads directory -->
    <external-path name="downloads" path="Download" />
    
    <!-- Access to app's external files directory -->
    <external-files-path name="external_files" path="." />
    
    <!-- Access to app's cache directory -->
    <cache-path name="cache" path="." />
    
    <!-- Access to app's files directory -->
    <files-path name="files" path="." />
    
    <!-- Access to external cache directory -->
    <external-cache-path name="external_cache" path="." />
    
    <!-- Access to external media files -->
    <external-media-path name="media" path="." />
    
    <!-- Access to all external storage (requires proper runtime permissions) -->
    <external-path name="external_storage" path="." />
</paths>